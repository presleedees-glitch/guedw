name: Chatbot

permissions:
  contents: write

on:
  push:
    paths:
      - inbox.json

jobs:
  reply:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Read inbox
        id: inbox
        run: |
          MSG=$(cat inbox.json | jq -r '.message')
          echo "msg=$MSG" >> $GITHUB_OUTPUT

      - name: Generate reply
        run: |
          MSG="${{ steps.inbox.outputs.msg }}"

          if [[ "$MSG" == *"hello"* ]]; then
            REPLY="Hi there!"
          elif [[ "$MSG" == *"how are you"* ]]; then
            REPLY="I'm running inside GitHub Actions, so I'm great."
          else
            REPLY="You said: $MSG"
          fi

          echo "{\"reply\": \"$REPLY\"}" > outbox.json

      - name: Commit reply
        run: |
          git config user.name "bot"
          git config user.email "bot@example.com"

          git pull --rebase

          git add outbox.json
          git commit -m "bot reply" || echo "No changes"
          git push
